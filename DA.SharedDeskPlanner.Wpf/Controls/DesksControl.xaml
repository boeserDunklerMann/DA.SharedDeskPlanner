<UserControl x:Class="DA.SharedDeskPlanner.Wpf.Controls.DesksControl"
			 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
			 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
			 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
			 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
			 xmlns:local="clr-namespace:DA.SharedDeskPlanner.Wpf.Controls"
			 xmlns:vm="clr-namespace:DA.SharedDeskPlanner.Wpf.MVVM"
			 mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
	<UserControl.DataContext>
		<vm:MainWindowViewModel/>
	</UserControl.DataContext>

	<Grid>
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="*"/>
		</Grid.ColumnDefinitions>
		<Grid.RowDefinitions>
			<RowDefinition Height="30"/>
			<RowDefinition Height="*"/>
			<RowDefinition Height="150"/>
		</Grid.RowDefinitions>
		<Label Grid.Row="0">Schreibtische</Label>
		<DataGrid Grid.Row="1" Width="auto" Background="LightCyan" ItemsSource="{Binding Desktops}" CanUserAddRows="False" CanUserDeleteRows="False"
				  SelectedItem="{Binding SelectedDesk}" AutoGenerateColumns="False">
			<DataGrid.Columns>
				<DataGridTextColumn Header="Bezeichnung" Binding="{Binding Name}"/>
				<DataGridTextColumn Header="Raum" Binding="{Binding Room.Name}"/>
				<DataGridTextColumn Header="angelegt am" Binding="{Binding CreationDate}"/>
				<DataGridTextColumn Header="letzte Änderung" Binding="{Binding ChangeDate}"/>
				<DataGridTemplateColumn Header="Inventar">
					<DataGridTemplateColumn.CellTemplate>
						<DataTemplate>
							<ListBox ItemsSource="{Binding Inventory}">
								
							</ListBox>
						</DataTemplate>
					</DataGridTemplateColumn.CellTemplate>
				</DataGridTemplateColumn>
				<DataGridTemplateColumn Header="Löschen">
					<DataGridTemplateColumn.CellTemplate>
						<DataTemplate>
							<Button Command="{Binding RelativeSource={RelativeSource AncestorType={x:Type local:DesksControl}}, Path=DataContext.DeleteDesk}">delete</Button>
						</DataTemplate>
					</DataGridTemplateColumn.CellTemplate>
				</DataGridTemplateColumn>
			</DataGrid.Columns>
		</DataGrid>
	</Grid>
</UserControl>
